package com.example.baseAlgotithm;

/**
 * @author lei
 * @create 2018/11/05 20:22.
 */
public class QuickSort {


    static int[] quickSort(final int[] list) {
        if (list.length < 2) {
            return list;
        }

        final int pivot = list[0];

        final int[] less = new int[list.length];
        final int[] great = new int[list.length];

        for (int i = 0; i < list.length; i++) {
            if (list[i] < pivot) {

            }

        }
        return null;
    }
}
